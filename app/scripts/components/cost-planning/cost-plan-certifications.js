import template from './cost-plan-certifications.html';

const costPlanCertifications = {
  template,
  bindings: {
    certifications: '<',
    plan: '<'
  }
};

export default costPlanCertifications;
