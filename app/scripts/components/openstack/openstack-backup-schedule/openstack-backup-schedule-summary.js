import template from './openstack-backup-schedule-summary.html';

const openstackBackupScheduleSummary = {
  template,
  bindings: {
    resource: '<'
  },
};

export default openstackBackupScheduleSummary;
