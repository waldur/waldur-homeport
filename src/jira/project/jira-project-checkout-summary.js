import template from './jira-project-checkout-summary.html';

const jiraProjectCheckoutSummary = {
  template: template,
  bindings: {
    model: '<'
  }
};

export default jiraProjectCheckoutSummary;
