// The spending-watch widgets reuse the shared dashboard chart palette so both
// experimental sections stay on the same theme-derived colors.
export { getChartThemeColors as getWatchColors } from '@/dashboard/chartColors';
