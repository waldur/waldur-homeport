// All @font-face declarations for both tenant-configurable FONT_FAMILY
// choices (Inter, Maven Pro) — see fonts.css's own comment for why both
// load unconditionally.
import 'waldur-design-tokens/fonts.css';

import 'text-security/text-security.css';
